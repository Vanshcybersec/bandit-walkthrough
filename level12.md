Level11 -> Level12

LEVEL GOAL:

e password for the next level is stored in the file data.txt, where all lowercase (a-z) and uppercase (A-Z) letters have been rotated by 13 positions.

WHAT I'VE LEARNED:

-In this level i learned to use`tr` or transpose command.

-`tr` is used for replacing or removing characters.

-I also learned about `ROT13`.

-I learned how we can use the effects of `ROT13` using `tr` command.

For more understanding of these commands you can use "command --help" in the terminal which will show how to use a command deeply and efficiently.


